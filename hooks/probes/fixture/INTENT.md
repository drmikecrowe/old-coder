# Intent for the probe fixture

The requester asked for a fixture that lets both halves of the
`spec-intent-scope` hook probe be run: one file inside the scope that the
reviewer may read, and a marker string that proves it actually read it rather
than inferring the contents.
