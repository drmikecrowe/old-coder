# Fixture spec — scribe-red-log (Tier 2)

Synthetic. One scenario; no merge gate.

## Scenarios

- `add(2, 2)` returns `4` — test: `tests/test_add.py::test_add`

## Setup plan

No files added, no dependencies. Entry point: `tools/gauntlet.sh <artifact dir>`
(runs tests only).
